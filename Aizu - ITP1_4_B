#include <stdio.h>

int main() {
    double r;
    scanf("%lf", &r);

    double pi = 3.14159265358979323846;
    double area = pi * r * r;
    double circumference = 2 * pi * r;

    printf("%.6f %.6f\n", area, circumference);

    return 0;
}
